import argparse

	estudiante = {
	     'nombre': args.nombre,
	}
  
	if __name__ == '__main__':

	parser=argparse.ArgumentParser()
	parser.add_argument('nombre')
	args=parser.parse_args()

	print(args.nombre)
	
	if nombre, args.nombre in estudiante.iteritems():
		print true
			else:
				print false



		